% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/screen.R
\name{ml_screen}
\alias{ml_screen}
\title{Screen all algorithms on your data}
\usage{
ml_screen(
  data,
  target,
  algorithms = NULL,
  seed = NULL,
  sort_by = "auto",
  time_budget = NULL,
  keep_models = TRUE,
  ...
)
}
\arguments{
\item{data}{An \code{ml_split_result} or \code{ml_cv_result} (NOT a raw data.frame --
split first to prevent overfitting)}

\item{target}{Target column name}

\item{algorithms}{Character vector of algorithm names, or NULL for all available}

\item{seed}{Random seed. NULL auto-generates.}

\item{sort_by}{"auto" (roc_auc for binary clf, f1_macro for multiclass,
rmse for regression), or a metric name string}

\item{time_budget}{Maximum seconds for entire screen. Stops between
algorithms (not mid-fit) when budget exceeded. NULL (default) = no limit.}

\item{keep_models}{If FALSE, discard fitted models after scoring to save
memory. ml_best() will return NULL. Default TRUE.}

\item{...}{Additional arguments passed to \code{\link[=ml_fit]{ml_fit()}}}
}
\value{
An object of class \code{ml_leaderboard} (data.frame with formatted print)
}
\description{
Fits every available algorithm on the training data and ranks by validation
performance. Use this to identify promising candidates before tuning.
}
\details{
\strong{Multiple comparison bias:} Selecting the best from N algorithms on the
same validation set produces optimistic estimates. The winning algorithm
benefits from selection bias. Use \code{\link[=ml_validate]{ml_validate()}} on held-out test data
for trustworthy comparisons.

For imbalanced data, consider \code{sort_by = "f1"} -- the default \code{roc_auc}
can hide failures on minority classes.
}
\examples{
\donttest{
s <- ml_split(iris, "Species", seed = 42)
lb <- ml_screen(s, "Species", seed = 42)
lb
}
}
