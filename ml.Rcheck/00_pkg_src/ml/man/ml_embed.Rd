% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embed.R
\name{ml_embed}
\alias{ml_embed}
\title{Embed texts into numeric features}
\usage{
ml_embed(texts, method = "tfidf", max_features = 100L)
}
\arguments{
\item{texts}{A character vector of texts to embed}

\item{method}{Embedding method. Currently only "tfidf" is supported.}

\item{max_features}{Maximum vocabulary size (number of TF-IDF features).
Default 100.}
}
\value{
An object of class \code{ml_embedder} with:
\itemize{
\item \verb{$vectors}: data.frame of TF-IDF features (n_texts x max_features)
\item \verb{$method}: the method used
\item \verb{$vocab_size}: number of features generated
\item \verb{$transform(new_texts)}: apply stored vocabulary to new texts
}
}
\description{
Fits a text vectorizer on training texts and returns an embedder object
that stores the vocabulary for consistent transform at prediction time.
}
\details{
Currently supports TF-IDF ('tm' package). SBERT and neural methods are
planned for future gates.
}
\examples{
\donttest{
# Requires 'tm' package: install.packages("tm")
texts <- c("good product", "bad service", "great value", "poor quality")
emb <- ml_embed(texts, method = "tfidf", max_features = 20)
emb$vocab_size
nrow(emb$vectors)

# Transform new texts using the fitted vocabulary
new_texts <- c("excellent quality", "terrible service")
new_vecs <- emb$transform(new_texts)
}
}
