% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{ml_compare}
\alias{ml_compare}
\title{Compare pre-fitted models on the same data}
\usage{
ml_compare(models, data, sort_by = "auto")
}
\arguments{
\item{models}{A list of \code{ml_model} objects (or \code{ml_tuning_result}, auto-unwrapped)}

\item{data}{A data.frame containing the target column}

\item{sort_by}{"auto" or a metric name string}
}
\value{
An object of class \code{ml_leaderboard} (data.frame with formatted print)
}
\description{
Evaluates multiple fitted models on the same dataset without re-fitting.
All models must share the same target column and task.
}
\examples{
\donttest{
s <- ml_split(iris, "Species", seed = 42)
m1 <- ml_fit(s$train, "Species", algorithm = "logistic", seed = 42)
m2 <- ml_fit(s$train, "Species", algorithm = "random_forest", seed = 42)
ml_compare(list(m1, m2), s$valid)
}
}
