% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{ml_plot}
\alias{ml_plot}
\title{Visual diagnostics for a fitted model}
\usage{
ml_plot(model, data = NULL, kind = "importance", ...)
}
\arguments{
\item{model}{An \code{ml_model} from \code{ml_fit()}}

\item{data}{A data.frame for computing predictions (required for all
except \code{"importance"})}

\item{kind}{Plot type. One of \code{"importance"}, \code{"roc"}, \code{"confusion"},
\code{"residual"}, \code{"calibration"}. Default: \code{"importance"}.}

\item{...}{Passed to the underlying base R plot call}
}
\value{
Invisibly returns NULL (called for its side effect)
}
\description{
Produces diagnostic plots using base R graphics. No extra packages required.
}
\details{
Available kinds:
\itemize{
\item \code{"importance"} -- feature importance bar chart
\item \code{"roc"} -- ROC curve (classification)
\item \code{"confusion"} -- confusion matrix heatmap (classification)
\item \code{"residual"} -- residuals vs fitted (regression)
\item \code{"calibration"} -- predicted vs actual probabilities (classification)
}
}
\examples{
\donttest{
s <- ml_split(iris, "Species", seed = 42)
model <- ml_fit(s$train, "Species", algorithm = "random_forest", seed = 42)
ml_plot(model, kind = "importance")
ml_plot(model, data = s$valid, kind = "confusion")
}
}
