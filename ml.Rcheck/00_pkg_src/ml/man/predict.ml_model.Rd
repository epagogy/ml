% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict.ml_model}
\alias{predict.ml_model}
\title{Predict from a fitted model}
\usage{
\method{predict}{ml_model}(object, newdata, proba = FALSE, ...)
}
\arguments{
\item{object}{An \code{ml_model} object}

\item{newdata}{A data.frame with the same features used for training}

\item{proba}{Logical. If TRUE, return class probabilities instead of labels
(classification only). Default FALSE.}

\item{...}{Ignored}
}
\value{
A vector of predicted class labels (classification) or numeric values
(regression). If \code{proba = TRUE}, returns a data.frame with one column per
class; values are probabilities summing to 1.0 per row.
}
\description{
Predict from a fitted model
}
\examples{
s <- ml_split(iris, "Species", seed = 42)
model <- ml_fit(s$train, "Species", seed = 42)
preds <- predict(model, newdata = s$valid)
head(preds)
}
