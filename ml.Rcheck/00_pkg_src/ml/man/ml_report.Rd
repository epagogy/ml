% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{ml_report}
\alias{ml_report}
\title{Generate an HTML training report}
\usage{
ml_report(model, data = NULL, path = tempfile(fileext = ".html"))
}
\arguments{
\item{model}{An \code{ml_model} from \code{ml_fit()}}

\item{data}{A data.frame for computing metrics (use validation data)}

\item{path}{Output file path. Defaults to a temporary file.}
}
\value{
The path to the saved report (invisibly)
}
\description{
Produces a self-contained HTML report with model metadata, evaluation
metrics, and feature importances. Open in any browser.
}
\examples{
\donttest{
s <- ml_split(iris, "Species", seed = 42)
model <- ml_fit(s$train, "Species", algorithm = "random_forest", seed = 42)
tmp <- tempfile(fileext = ".html")
ml_report(model, data = s$valid, path = tmp)
unlink(tmp)
}
}
