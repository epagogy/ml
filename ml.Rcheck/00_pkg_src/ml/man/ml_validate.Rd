% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{ml_validate}
\alias{ml_validate}
\title{Validate model against rules and/or baseline}
\usage{
ml_validate(model, test, rules = NULL, baseline = NULL, tolerance = 0)
}
\arguments{
\item{model}{An \code{ml_model}}

\item{test}{Test data.frame (use \code{s$test})}

\item{rules}{Named list of threshold strings, e.g.
\code{list(accuracy = ">0.85", roc_auc = ">=0.90")}}

\item{baseline}{An \code{ml_model} -- previous model to check for regressions}

\item{tolerance}{Numeric. Allowed absolute degradation (0.02 = 2pp slack).
Default 0.0.}
}
\value{
An object of class \code{ml_validate_result}
}
\description{
Three modes: (1) absolute rules, (2) regression prevention vs baseline,
(3) combined. Returns a structured result with pass/fail and diagnostics.
}
\details{
Tolerance is absolute (not relative): a tolerance of 0.02 means 2 percentage
points of allowed degradation, applied uniformly across all metrics.
}
\examples{
\donttest{
s <- ml_split(iris, "Species", seed = 42)
model <- ml_fit(s$train, "Species", seed = 42)
gate <- ml_validate(model, test = s$test, rules = list(accuracy = ">0.80"))
gate$passed
}
}
