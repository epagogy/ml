% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{ml_evaluate}
\alias{ml_evaluate}
\title{Evaluate model on validation data (iterate freely)}
\usage{
ml_evaluate(model, data)
}
\arguments{
\item{model}{An \code{ml_model} or \code{ml_tuning_result}}

\item{data}{A data.frame containing the target column}
}
\value{
An object of class \code{ml_metrics} (named numeric vector with print method)
}
\description{
The practice exam -- call as many times as needed. For the one-time final
grade on held-out test data, use \code{\link[=ml_assess]{ml_assess()}}.
}
\examples{
s <- ml_split(iris, "Species", seed = 42)
model <- ml_fit(s$train, "Species", seed = 42)
metrics <- ml_evaluate(model, s$valid)
metrics[["accuracy"]]
}
