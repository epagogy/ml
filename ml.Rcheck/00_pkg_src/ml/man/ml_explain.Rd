% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explain.R
\name{ml_explain}
\alias{ml_explain}
\title{Explain model via feature importance}
\usage{
ml_explain(model)
}
\arguments{
\item{model}{An \code{ml_model} or \code{ml_tuning_result}}
}
\value{
An object of class \code{ml_explanation} (a data.frame with columns
\code{feature} and \code{importance}; custom print shows a bar chart)
}
\description{
Quantifies each feature's contribution to a fitted model, answering
"which features drive predictions?". Returns a data.frame of importances
normalized to sum to 1.0, sorted descending.
}
\details{
The extraction method depends on the algorithm:
\itemize{
\item Tree ensembles ('random_forest', 'xgboost', 'gradient_boosting',
'decision_tree', 'extra_trees', 'histgradient', 'adaboost'):
impurity-based importance (mean decrease in split criterion).
\item Linear models ('logistic', 'linear', 'elastic_net'):
absolute coefficient magnitude. For multiclass logistic, coefficients
are averaged across one-vs-rest classifiers.
\item Not supported: 'svm', 'knn', 'naive_bayes' (no intrinsic
importance measure). Use permutation importance instead.
}
}
\examples{
\donttest{
s <- ml_split(iris, "Species", seed = 42)
model <- ml_fit(s$train, "Species", algorithm = "random_forest", seed = 42)
ml_explain(model)
}
}
