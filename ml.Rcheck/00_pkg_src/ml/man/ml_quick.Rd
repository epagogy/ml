% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quick.R
\name{ml_quick}
\alias{ml_quick}
\title{One-call workflow: split + screen + fit + evaluate}
\usage{
ml_quick(data, target, seed)
}
\arguments{
\item{data}{A data.frame with features and target}

\item{target}{Target column name}

\item{seed}{Random seed}
}
\value{
A list with \code{model} (ml_model), \code{metrics} (ml_metrics),
and \code{split} (ml_split_result).
}
\description{
The fastest path from raw data to a trained, evaluated model.
Screens logistic, random_forest, and xgboost, picks the best,
fits on training data, and evaluates on validation.
}
\examples{
result <- ml_quick(iris, "Species", seed = 42)
result$model
result$metrics
}
