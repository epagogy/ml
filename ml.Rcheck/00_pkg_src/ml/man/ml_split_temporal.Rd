% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split.R
\name{ml_split_temporal}
\alias{ml_split_temporal}
\title{Split data chronologically -- no future leakage}
\usage{
ml_split_temporal(
  data,
  target = NULL,
  time,
  ratio = c(0.6, 0.2, 0.2),
  folds = NULL,
  task = "auto"
)
}
\arguments{
\item{data}{A data.frame}

\item{target}{Target column name (optional, enables task detection)}

\item{time}{Column name containing timestamps or orderable values.
Used for sorting, then dropped from output partitions.}

\item{ratio}{Numeric vector c(train, valid, test). Must sum to 1.0.}

\item{folds}{Integer for temporal CV (expanding window). When set, ignores ratio.}

\item{task}{"auto", "classification", or "regression"}
}
\value{
An \code{ml_split_result} (holdout) or \code{ml_cv_result} (temporal CV)
}
\description{
Domain specialization of \code{ml_split()} for time series and forecasting.
Data is sorted by the \code{time} column and partitioned by position.
Deterministic: seed is ignored (chronological order is the only order).
}
\examples{
df <- data.frame(date = 1:100, x = rnorm(100), y = sample(0:1, 100, TRUE))
s <- ml_split_temporal(df, "y", time = "date")
nrow(s$train)
}
