% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{ml_save}
\alias{ml_save}
\title{Save a model to disk}
\usage{
ml_save(model, path)
}
\arguments{
\item{model}{An \code{ml_model} or \code{ml_tuning_result}}

\item{path}{File path (recommended extension: \code{.mlr})}
}
\value{
The normalized path, invisibly.
}
\description{
Saves an \code{ml_model} or \code{ml_tuning_result} to a \code{.mlr} file using
\code{\link[base]{saveRDS}} with a version wrapper.
}
\section{Security}{

\code{ml_load()} uses \code{readRDS()} internally, which can execute
arbitrary R code during deserialization. Never load \code{.mlr} files
from untrusted sources.
}

\examples{
\donttest{
s <- ml_split(iris, "Species", seed = 42)
model <- ml_fit(s$train, "Species", seed = 42)
path <- file.path(tempdir(), "iris_model.mlr")
ml_save(model, path)
loaded <- ml_load(path)
}
}
