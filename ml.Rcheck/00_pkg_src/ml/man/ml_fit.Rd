% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{ml_fit}
\alias{ml_fit}
\title{Fit a machine learning model}
\usage{
ml_fit(
  data,
  target,
  algorithm = "auto",
  seed = NULL,
  task = "auto",
  balance = FALSE,
  engine = "auto",
  ...
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{ml_cv_result} (for cross-validation)}

\item{target}{Target column name (string)}

\item{algorithm}{"auto" (default), "logistic", "decision_tree",
"random_forest", "extra_trees", "gradient_boosting", "xgboost",
"linear", "elastic_net", "svm", "knn", "naive_bayes", "adaboost",
"histgradient". See \code{\link[=ml_algorithms]{ml_algorithms()}} for availability.}

\item{seed}{Random seed. NULL (default) auto-generates and stores for
reproducibility.}

\item{task}{"auto", "classification", or "regression"}

\item{balance}{Logical. If \code{TRUE}, applies class-weight balancing for
imbalanced classification problems. Ignored for regression. Default: \code{FALSE}.}

\item{engine}{Backend engine: \code{"auto"} (Rust if available, else CRAN packages),
\code{"ml"} (Rust required), or \code{"r"} (CRAN packages only). Default: \code{"auto"}.}

\item{...}{Additional hyperparameters passed to the engine
(e.g., \code{max_depth = 6}, \code{num.trees = 200})}
}
\value{
An object of class \code{ml_model}
}
\description{
Trains a model using cross-validation (if \code{data} is an \code{ml_cv_result}) or
holdout (if \code{data} is a \code{data.frame}). Automatically detects task type,
handles encoding, and records metadata for reproducibility.
}
\details{
Formula interfaces are not supported. Pass the data frame and target column
name as a string. Unordered factors use one-hot encoding for linear models
and ordinal encoding for tree-based models. Ordered factors always use
ordinal encoding.
}
\examples{
s <- ml_split(iris, "Species", seed = 42)
model <- ml_fit(s$train, "Species", seed = 42)
model$algorithm
}
