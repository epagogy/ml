% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{ml_predict}
\alias{ml_predict}
\title{Predict from a fitted model (ml_predict style)}
\usage{
ml_predict(model, new_data)
}
\arguments{
\item{model}{An \code{ml_model} or \code{ml_tuning_result}}

\item{new_data}{A data.frame with the same features used for training}
}
\value{
A vector of predicted class labels (classification) or numeric values
(regression).
}
\description{
Alias for \code{predict(model, newdata = ...)}. Matches Python \code{ml.predict()}.
}
\examples{
s <- ml_split(iris, "Species", seed = 42)
model <- ml_fit(s$train, "Species", seed = 42)
preds <- ml_predict(model, s$valid)
head(preds)
}
