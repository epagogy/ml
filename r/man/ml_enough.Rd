% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enough.R
\name{ml_enough}
\alias{ml_enough}
\title{Learning curve analysis -- do you need more data?}
\usage{
ml_enough(s, target, seed = NULL, algorithm = "auto", steps = 8L, cv = 3L)
}
\arguments{
\item{s}{An \code{ml_split_result} from \code{ml_split()}}

\item{target}{Target column name}

\item{seed}{Random seed (optional in R; auto-generated if NULL)}

\item{algorithm}{Algorithm to use (default \code{"auto"}). Any algorithm
supported by \code{ml_fit()}.}

\item{steps}{Integer >= 2. Number of data-size steps to evaluate, evenly
spaced from ~10\%\% to 100\%\% of training data. Default 8.}

\item{cv}{Integer >= 2. Number of cross-validation folds for validation
score at each step. Default 3.}
}
\value{
An \code{ml_enough_result} with fields:
\itemize{
\item \verb{$saturated} -- logical, TRUE if curve plateaus (< 1\%\% gain in last half)
\item \verb{$curve} -- data.frame: n_samples, train_score, val_score
\item \verb{$metric} -- metric name used
\item \verb{$n_current} -- total training rows in the full dataset
\item \verb{$recommendation} -- human-readable action
}
}
\description{
Trains at increasing data sizes and reports train vs validation performance
at each step. Answers: is the model still learning (more data helps), or
saturated (more data unlikely to help)?
}
\examples{
s <- ml_split(iris, "Species", seed = 42)
result <- ml_enough(s, "Species", seed = 42)
result$recommendation
}
