% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.R
\name{ml_cv}
\alias{ml_cv}
\title{Create k-fold cross-validation from a split}
\usage{
ml_cv(s, target, folds = 5L, seed = NULL, stratify = TRUE)
}
\arguments{
\item{s}{An \code{ml_split_result} from \code{ml_split()}}

\item{target}{Target column name (string)}

\item{folds}{Number of folds (default 5)}

\item{seed}{Random seed for fold assignment}

\item{stratify}{Logical. Stratify folds by target for classification (default TRUE)}
}
\value{
An \code{ml_cv_result} that \code{ml_fit()} accepts directly.
The original split's \verb{$test} remains available via \code{s$test} for \code{ml_assess()}.
}
\description{
Takes an existing \code{ml_split_result} and creates k-fold rotations within its
dev partition (train + valid). The test partition stays sealed on the original
split for \code{ml_assess()}.
}
\details{
Two primitives, strict separation of concerns: \code{ml_split()} creates the
three-way boundary, \code{ml_cv()} creates rotations within that boundary.
}
\examples{
s <- ml_split(iris, "Species", seed = 42)
c <- ml_cv(s, "Species", folds = 5, seed = 42)
model <- ml_fit(c, "Species", seed = 42)
model$scores_
}
