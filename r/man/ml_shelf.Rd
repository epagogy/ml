% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shelf.R
\name{ml_shelf}
\alias{ml_shelf}
\title{Check if a model is past its shelf life}
\usage{
ml_shelf(model, new, target, tolerance = 0.05)
}
\arguments{
\item{model}{An \code{ml_model} or \code{ml_tuning_result} with \verb{$scores_} populated
(i.e., trained with \code{ml_fit(cv_result, target)})}

\item{new}{A data.frame — new labeled dataset including the target column}

\item{target}{Name of the target column in \code{new}}

\item{tolerance}{Allowed degradation per metric (default 0.05 = 5pp).
Any key metric degrading beyond tolerance marks the model as stale.}
}
\value{
An object of class \code{ml_shelf_result} with:
\itemize{
\item \verb{$fresh}: TRUE if model performance is within tolerance
\item \verb{$stale}: inverse of fresh
\item \verb{$metrics_then}: original training metrics (from model$scores_)
\item \verb{$metrics_now}: current metrics on new data
\item \verb{$degradation}: per-metric delta (negative = worse for higher-is-better)
\item \verb{$recommendation}: human-readable guidance
}
}
\description{
Evaluates the model on new labeled data and compares performance to the
model's original training metrics. Requires ground truth labels.
}
\details{
Run this when outcome labels become available (e.g., daily/weekly batch
scoring, then wait for outcomes). Pair with \code{\link[=ml_drift]{ml_drift()}} for complete
monitoring:
\itemize{
\item \code{\link[=ml_drift]{ml_drift()}}: input distribution shift (label-free, run always)
\item \code{\link[=ml_shelf]{ml_shelf()}}: performance degradation (needs labels, run periodically)
}

\strong{Requires} \code{model$scores_} from a cross-validated fit. If the model was
trained on a holdout split (no CV), \code{scores_} will be NULL and shelf()
raises a model_error.
}
\examples{
\donttest{
cv    <- ml_split(iris, "Species", seed = 42, folds = 3)
model <- ml_fit(cv, "Species", algorithm = "logistic", seed = 42)
# Simulate a new labeled batch
new_batch <- iris[sample(nrow(iris), 30), ]
result <- ml_shelf(model, new = new_batch, target = "Species")
result$fresh
result$degradation
}
}
