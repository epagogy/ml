% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.R
\name{ml_cv_temporal}
\alias{ml_cv_temporal}
\title{Create temporal cross-validation from a split}
\usage{
ml_cv_temporal(
  s,
  target,
  folds = 5L,
  embargo = 0L,
  window = "expanding",
  window_size = NULL
)
}
\arguments{
\item{s}{An \code{ml_split_result} from \code{ml_split_temporal()}}

\item{target}{Target column name (string)}

\item{folds}{Number of folds (default 5)}

\item{embargo}{Integer. Number of rows to skip between train end and valid
start (gap to prevent temporal leakage from autocorrelation). Default 0.
Must be >= 0.}

\item{window}{\code{"expanding"} (default, all prior rows as train) or
\code{"sliding"} (fixed-size training window). When \code{"sliding"},
\code{window_size} must also be supplied.}

\item{window_size}{Integer. Required when \code{window = "sliding"}. Number of
rows in each training window.}
}
\value{
An \code{ml_cv_result} with expanding-window folds
}
\description{
Expanding-window CV for time series. Data must already be sorted
chronologically (use \code{ml_split_temporal()} first).
}
\examples{
df <- data.frame(date = 1:100, x = rnorm(100), y = sample(0:1, 100, TRUE))
s <- ml_split_temporal(df, "y", time = "date")
c <- ml_cv_temporal(s, "y", folds = 5)
# With embargo to prevent autocorrelation leakage:
c2 <- ml_cv_temporal(s, "y", folds = 5, embargo = 5L)
}
