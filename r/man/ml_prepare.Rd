% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare.R
\name{ml_prepare}
\alias{ml_prepare}
\title{Prepare data for ML: encode, impute, and scale}
\usage{
ml_prepare(data, target, algorithm = "auto", task = "auto")
}
\arguments{
\item{data}{A data.frame including the target column.}

\item{target}{Name of the target column (string).}

\item{algorithm}{Algorithm hint for encoding strategy: "auto", "random_forest",
"logistic", etc. Tree-based algorithms use ordinal encoding; linear algorithms
use one-hot encoding for low-cardinality categoricals.}

\item{task}{"classification", "regression", or "auto" (detected from target).}
}
\value{
An \code{ml_prepared_data} object with:
\itemize{
\item \verb{$data}   — transformed data.frame (all-numeric, ready for ml_fit)
\item \verb{$state}  — NormState list; use .transform(state, X) on new data
\item \verb{$target} — target column name
\item \verb{$task}   — detected or provided task type
}
}
\description{
Grammar primitive #2: DataFrame -> PreparedData.
}
\details{
In the default workflow, \code{ml_fit()} calls preparation internally per fold.
Use \code{ml_prepare()} explicitly when you need manual control: inspect the
preprocessing state, apply the same encoding to external data, or chain
preparation with fitting.
}
\examples{
\donttest{
df <- data.frame(x1 = rnorm(50), x2 = rnorm(50), y = rnorm(50))
s <- ml_split(df, "y", seed = 42)
p <- ml_prepare(s$train, "y")
p$task       # "classification" or "regression"
p$data       # encoded feature matrix
}

}
