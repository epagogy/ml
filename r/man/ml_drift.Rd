% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drift.R
\name{ml_drift}
\alias{ml_drift}
\title{Detect data drift between reference and new data}
\usage{
ml_drift(
  reference,
  new,
  method = "statistical",
  threshold = 0.05,
  exclude = NULL,
  target = NULL,
  seed = NULL,
  algorithm = "random_forest"
)
}
\arguments{
\item{reference}{A data.frame — reference dataset (typically training data)}

\item{new}{A data.frame — new data to compare against the reference}

\item{method}{Detection method: "statistical" (default) or "adversarial"}

\item{threshold}{p-value threshold for statistical method (default 0.05)}

\item{exclude}{Character vector of column names to skip (e.g., ID columns)}

\item{target}{Target column name — automatically excluded from drift analysis}

\item{seed}{Random seed (required for method = "adversarial")}

\item{algorithm}{Algorithm for adversarial classifier: "random_forest"
(default) or "xgboost"}
}
\value{
An object of class \code{ml_drift_result} with:
\itemize{
\item \verb{$shifted}: TRUE if drift detected
\item \verb{$features}: named numeric — p-values (statistical) or importances (adversarial)
\item \verb{$features_shifted}: character vector of drifted feature names
\item \verb{$severity}: "none", "low", "medium", or "high"
\item \verb{$auc}: adversarial mode only — classifier AUC
\item \verb{$train_scores}: adversarial mode only — per-row reference probabilities
}
}
\description{
Compares a reference dataset (typically training data) to new data using
per-feature statistical tests or adversarial validation.
}
\details{
\strong{Statistical method} (default): per-feature distribution tests with no
labels required.
\itemize{
\item Numeric features: Kolmogorov-Smirnov two-sample test
\item Categorical features: Chi-squared test on value counts
}

\strong{Adversarial method}: trains a binary classifier to distinguish reference
from new data. AUC near 0.5 means similar distributions; AUC near 1.0 means
very different distributions.
\itemize{
\item \verb{$train_scores}: per-row probability of "looks like new data" for reference
rows. Use \code{sort(result$train_scores, decreasing = TRUE)[1:n]} to select
validation rows that mirror the new distribution.
\item \verb{$features}: most discriminative features (temporal leakage candidates)
}

Pair with \code{\link[=ml_shelf]{ml_shelf()}} for complete monitoring: drift() detects input
distribution shift (label-free), shelf() detects performance degradation
(requires labels).
}
\examples{
s    <- ml_split(iris, "Species", seed = 42)
# Simulate drift by perturbing test data
new  <- s$test
new$Sepal.Length <- new$Sepal.Length + 2
result <- ml_drift(reference = s$train, new = new, target = "Species")
result$shifted
result$features_shifted
}
