% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimize.R
\name{ml_optimize}
\alias{ml_optimize}
\title{Optimize decision threshold for binary classification}
\usage{
ml_optimize(model, data, metric = "f1", min_threshold = "auto")
}
\arguments{
\item{model}{An \code{ml_model} or \code{ml_tuning_result} (binary classification only).}

\item{data}{A data.frame containing the target column used as true labels.}

\item{metric}{Character. Optimisation objective: \code{"f1"}, \code{"accuracy"},
\code{"precision"}, or \code{"recall"}. Ranking metrics (\code{"roc_auc"}, \code{"log_loss"})
are rejected because they are threshold-independent.}

\item{min_threshold}{Lower bound of the sweep. \code{"auto"} (default) computes
\code{max(0.001, 1 / n_positives)} -- the minimum meaningful threshold for
imbalanced data. Pass a numeric value to override.}
}
\value{
An \code{ml_optimize_result} (also an \code{ml_model}). The threshold is
baked in -- every \code{ml_predict()} call uses it automatically.
Inspect with \code{result$threshold}. The original model is unchanged.
}
\description{
Sweeps thresholds from \code{min_threshold} to 0.95 in two phases (coarse
0.05 steps, then fine 0.005 steps around the coarse best) and returns a
copy of the model with a tuned threshold. Subsequent \code{ml_predict()} calls
apply this threshold to positive-class probability instead of 0.5.
}
\examples{
s     <- ml_split(iris[iris$Species != "virginica", ], "Species", seed = 42)
model <- ml_fit(s$train, "Species", seed = 42)
opt   <- ml_optimize(model, data = s$valid, metric = "f1")
opt$threshold
}
