% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.R
\name{ml_cv_group}
\alias{ml_cv_group}
\title{Create group-aware cross-validation from a split}
\usage{
ml_cv_group(s, target, groups, folds = 5L, seed = NULL)
}
\arguments{
\item{s}{An \code{ml_split_result} from \code{ml_split()} or \code{ml_split_group()}}

\item{target}{Target column name (string)}

\item{groups}{Column name identifying groups}

\item{folds}{Number of folds (default 5)}

\item{seed}{Random seed for group assignment}
}
\value{
An \code{ml_cv_result} with group-aware folds
}
\description{
No group appears in both train and validation within any fold.
Prevents leakage from repeated measurements (patients, stores, sensors).
}
\examples{
df <- data.frame(pid = rep(1:20, each = 5), x = rnorm(100), y = sample(0:1, 100, TRUE))
s <- ml_split(df, "y", seed = 42)
c <- ml_cv_group(s, "y", groups = "pid", folds = 5, seed = 42)
}
