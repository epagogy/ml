% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split.R
\name{ml_split}
\alias{ml_split}
\title{Split data into train/valid/test partitions or cross-validation folds}
\usage{
ml_split(
  data,
  target = NULL,
  seed = NULL,
  ratio = c(0.6, 0.2, 0.2),
  folds = NULL,
  stratify = TRUE,
  task = "auto",
  time = NULL,
  groups = NULL
)
}
\arguments{
\item{data}{A data.frame (also accepts tibble or data.table)}

\item{target}{Target column name (enables stratification + task detection)}

\item{seed}{Random seed. NULL (default) auto-generates and stores for
reproducibility. Pass an integer for reproducible splits.}

\item{ratio}{Numeric vector of length 3: c(train, valid, test). Must sum to 1.0.}

\item{folds}{Integer for k-fold CV (e.g., \code{folds = 5}). Overrides ratio.}

\item{stratify}{Logical. Auto-stratify for classification targets (default TRUE).}

\item{task}{"auto", "classification", or "regression". Override task detection.}

\item{time}{Column name for temporal/chronological split. Data is sorted by
this column, and the time column is dropped from output. Deterministic
(seed is ignored). Cannot combine with \code{groups}.}

\item{groups}{Column name for group-aware split. No group appears in both
train and validation/test. Cannot combine with \code{time}.}
}
\value{
An \code{ml_split_result}. Access \verb{$train}, \verb{$valid}, \verb{$test},
\verb{$dev} (train + valid). When \code{folds} is set, also \verb{$folds} (CV on dev).
}
\description{
Three-way split is the default (60/20/20), following Hastie, Tibshirani, and
Friedman (2009, ISBN:978-0-387-84857-0) Chapter 7. Automatically stratifies
for classification.
}
\examples{
s <- ml_split(iris, "Species", seed = 42)
nrow(s$train)
nrow(s$dev)
}
