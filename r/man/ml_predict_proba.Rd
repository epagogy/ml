% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{ml_predict_proba}
\alias{ml_predict_proba}
\title{Predict class probabilities}
\usage{
ml_predict_proba(model, new_data)
}
\arguments{
\item{model}{An \code{ml_model} object (classification only)}

\item{new_data}{A data.frame with the same features used for training}
}
\value{
A data.frame with one column per class. Values are probabilities
summing to 1.0 per row. Column names are the original class labels.
}
\description{
Predict class probabilities
}
\examples{
s <- ml_split(iris, "Species", seed = 42)
model <- ml_fit(s$train, "Species", algorithm = "random_forest", seed = 42)
probs <- ml_predict_proba(model, s$valid)
head(probs)
}
