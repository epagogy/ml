% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assess.R
\name{ml_assess}
\alias{ml_assess}
\title{Assess model on held-out test data (do once)}
\usage{
ml_assess(model, test)
}
\arguments{
\item{model}{An \code{ml_model}}

\item{test}{Test data.frame (use \code{s$test}). Specify by name for clarity.}
}
\value{
An object of class \code{ml_evidence} (sealed — not substitutable for \code{ml_metrics})
}
\description{
The final exam — separate from \code{\link[=ml_evaluate]{ml_evaluate()}} to force a conscious choice.
Errors if called more than once on the same model. Use \code{s$test} (not
\code{s$valid}) for the test data.
}
\examples{
s <- ml_split(iris, "Species", seed = 42)
model <- ml_fit(s$train, "Species", seed = 42)
verdict <- ml_assess(model, test = s$test)
}
