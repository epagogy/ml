% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verify.R
\name{ml_verify}
\alias{ml_verify}
\title{Verify provenance integrity of a model}
\usage{
ml_verify(model)
}
\arguments{
\item{model}{An \code{ml_model}, \code{ml_tuning_result}, or path to \code{.mlr} file}
}
\value{
A list with \code{status} ("verified"/"unverified"/"warning"),
\code{checks}, \code{provenance}, and \code{assess_count}.
}
\description{
Checks provenance chain: split parameters -> training fingerprint ->
assess ceremony status. Catches accidental self-deception (load-assess
loops, test-set shopping) rather than adversarial tampering.
}
\examples{
s <- ml_split(iris, "Species", seed = 42)
model <- ml_fit(s$train, "Species", seed = 42)
report <- ml_verify(model)
report$status
}
