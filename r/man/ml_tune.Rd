% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune.R
\name{ml_tune}
\alias{ml_tune}
\title{Tune hyperparameters via random or grid search}
\usage{
ml_tune(
  data,
  target,
  model = NULL,
  algorithm = NULL,
  n_trials = 20L,
  cv_folds = 3L,
  method = "random",
  seed = NULL,
  params = NULL
)
}
\arguments{
\item{data}{A data.frame or \code{ml_split_result}}

\item{target}{Target column name}

\item{model}{An \code{ml_model} object (to clone algorithm from), or NULL}

\item{algorithm}{Algorithm name (if model is NULL)}

\item{n_trials}{Number of random search trials (default 20)}

\item{cv_folds}{Number of CV folds per trial (default 3)}

\item{method}{"random" (default) or "grid"}

\item{seed}{Random seed}

\item{params}{Named list of parameter ranges (overrides defaults). For numeric
ranges, provide a 2-element numeric vector c(min, max). For discrete,
provide a character/integer vector.}
}
\value{
An object of class \code{ml_tuning_result}
}
\description{
Tune hyperparameters via random or grid search
}
\examples{
\donttest{
s <- ml_split(iris, "Species", seed = 42)
tuned <- ml_tune(s$train, "Species", algorithm = "xgboost", n_trials = 5, seed = 42)
tuned$best_params_
}
}
