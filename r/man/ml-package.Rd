% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml-package.R
\docType{package}
\name{ml-package}
\alias{ml-package}
\title{ml: Machine Learning Workflows Made Simple}
\description{
Implements the split-fit-evaluate-assess workflow from Hastie, Tibshirani,
and Friedman (2009, ISBN:978-0-387-84857-0) 'The Elements of Statistical
Learning', Chapter 7. Provides three-way data splitting with automatic
stratification, mandatory seeds for reproducibility, automatic data type
handling, and 8 algorithms out of the box.
\subsection{Workflow}{

\if{html}{\out{<div class="sourceCode r">}}\preformatted{library(ml)

s       <- ml_split(iris, "Species", seed = 42)
model   <- ml_fit(s$train, "Species", seed = 42)
metrics <- ml_evaluate(model, s$valid)
verdict <- ml_assess(model, test = s$test)
}\if{html}{\out{</div>}}
}

\subsection{API}{

All functions are available both as standalone \code{ml_verb()} functions and as
\code{ml$verb()} module-style calls. Both styles are equivalent.\tabular{ll}{
   Function \tab What it does \cr
   \code{\link[=ml_split]{ml_split()}} \tab Three-way train/valid/test split \cr
   \code{\link[=ml_fit]{ml_fit()}} \tab Fit a model \cr
   \code{\link[=ml_evaluate]{ml_evaluate()}} \tab Evaluate on validation data (iterate freely) \cr
   \code{\link[=ml_assess]{ml_assess()}} \tab Assess on test data (do once) \cr
   \code{\link[=ml_predict_proba]{ml_predict_proba()}} \tab Class probabilities \cr
   \code{\link[=ml_explain]{ml_explain()}} \tab Feature importance \cr
   \code{\link[=ml_screen]{ml_screen()}} \tab Compare all algorithms quickly \cr
   \code{\link[=ml_compare]{ml_compare()}} \tab Compare fitted models \cr
   \code{\link[=ml_tune]{ml_tune()}} \tab Hyperparameter tuning \cr
   \code{\link[=ml_stack]{ml_stack()}} \tab Ensemble stacking \cr
   \code{\link[=ml_validate]{ml_validate()}} \tab Validation gate with rules \cr
   \code{\link[=ml_profile]{ml_profile()}} \tab Data profiling and warnings \cr
   \code{\link[=ml_save]{ml_save()}} / \code{\link[=ml_load]{ml_load()}} \tab Model serialization (.mlr format) \cr
   \code{\link[=ml_algorithms]{ml_algorithms()}} \tab List available algorithms \cr
   \code{\link[=ml_dataset]{ml_dataset()}} \tab Built-in datasets \cr
}

}

\subsection{Algorithms}{\tabular{lccl}{
   Algorithm \tab Classification \tab Regression \tab Package \cr
   "xgboost" \tab yes \tab yes \tab 'xgboost' \cr
   "random_forest" \tab yes \tab yes \tab 'ranger' \cr
   "logistic" \tab yes \tab — \tab base R \cr
   "linear" (Ridge) \tab — \tab yes \tab 'glmnet' \cr
   "elastic_net" \tab — \tab yes \tab 'glmnet' \cr
   "svm" \tab yes \tab yes \tab 'e1071' \cr
   "knn" \tab yes \tab yes \tab 'kknn' \cr
   "naive_bayes" \tab yes \tab — \tab 'naivebayes' \cr
}


LightGBM is available in Python 'mlw'. R support is planned for v1.1.
}

\subsection{Notes}{
\itemize{
\item Formula interfaces are not supported. Pass the data frame and target column
name as a string: \code{ml_fit(data, "target", seed = 42)}.
\item Seeds are optional (default NULL auto-generates) but recommended for
reproducibility.
}
}
}
\seealso{
Useful links:
\itemize{
  \item \url{https://github.com/epagogy/ml}
  \item \url{https://epagogy.ai}
  \item Report bugs at \url{https://github.com/epagogy/ml/issues}
}

}
\author{
\strong{Maintainer}: Simon Roth \email{simon@epagogy.ai}

}
\keyword{internal}
