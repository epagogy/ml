% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split.R
\name{ml_split_group}
\alias{ml_split_group}
\title{Split data with group non-overlap — no group leaks across partitions}
\usage{
ml_split_group(
  data,
  target = NULL,
  groups,
  seed = NULL,
  ratio = c(0.6, 0.2, 0.2),
  folds = NULL,
  stratify = TRUE,
  task = "auto"
)
}
\arguments{
\item{data}{A data.frame}

\item{target}{Target column name (optional, enables stratification)}

\item{groups}{Column name identifying groups}

\item{seed}{Random seed for reproducibility}

\item{ratio}{Numeric vector c(train, valid, test). Must sum to 1.0.}

\item{folds}{Integer for group CV. When set, ignores ratio.}

\item{stratify}{Logical. Stratify by target within groups (default TRUE).}

\item{task}{"auto", "classification", or "regression"}
}
\value{
An \code{ml_split_result}. When \code{folds} is set, includes \verb{$folds} and \verb{$test}.
}
\description{
Domain specialization of \code{ml_split()} for clinical trials, repeated measures,
and any data where observations are nested within groups (patients, subjects,
hospitals). No group appears in more than one partition.
}
\details{
Also covers Leave-Source-Out CV: when groups represent data sources
(hospitals, devices), this produces deployment-realistic evaluation.
}
\examples{
df <- data.frame(pid = rep(1:10, each = 5), x = rnorm(50), y = sample(0:1, 50, TRUE))
s <- ml_split_group(df, "y", groups = "pid", seed = 42)
nrow(s$train)
}
